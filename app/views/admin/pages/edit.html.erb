<h2>Edit Page</h2>
<hr>

<%= simple_form_for(@page, url: admin_page_path(@page), method: :put) do |f| %>
  <%= render 'form', :f => f %>
<% end %>

<%= link_to 'Delete Page', admin_page_path(@page), method: :delete, class: 'delete', "data-confirm" => "Are you sure you want to delete this?" %>