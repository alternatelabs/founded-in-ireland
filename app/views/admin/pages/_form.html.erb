<%= simple_form_for([:admin, @page]) do |f| %>
  <%= f.input :title %>
  <%= f.input :slug, label: "Slug (this cannot be updated)" unless @page.id.present? %>
  <%= f.input :content, input_html: { class: 'wysiwyg' } %>

  <hr>

  <%= f.button :submit %>
<% end %>